apiVersion: v1
kind: ResourceQuota

metadata:
  name: {{ include "devops-app.fullname" . }}

spec:
  hard:

    requests.cpu: "2"

    requests.memory: 4Gi

    limits.cpu: "4"

    limits.memory: 8Gi

    pods: "20"

    services: "10"

    persistentvolumeclaims: "5"