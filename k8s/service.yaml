apiVersion: v1
kind: Service
metadata:
  name: devops-app
  namespace: dev
  labels:
    app.kubernetes.io/name: devops-app
    app.kubernetes.io/instance: devops-app
spec:
  selector:
    app.kubernetes.io/name: devops-app
    app.kubernetes.io/instance: devops-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
