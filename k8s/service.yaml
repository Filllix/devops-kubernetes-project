apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-app.fullname" . }}
  namespace: dev
spec:
  selector:
    app: {{ include "devops-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
  type: ClusterIP