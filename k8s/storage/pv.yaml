apiVersion: v1
kind: PersistentVolume

metadata:
  name: devops-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /tmp/devops-storage