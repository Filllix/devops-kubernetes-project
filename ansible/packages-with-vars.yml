---
- name: Install DevOps Packages Using Variables
  hosts: local
  become: true

  vars_files:
    - vars.yml

  tasks:
    - name: Install required DevOps packages
      ansible.builtin.apt:
        name: "{{ devops_packages }}"
        state: present
        update_cache: true
